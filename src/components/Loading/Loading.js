import React from "react";
import "./Loading.scss"; // Thêm CSS cho loading

const Loading = () => {
  return (
    <div className="loading">
      <div className="loader"></div>
    </div>
  );
};

export default Loading;
